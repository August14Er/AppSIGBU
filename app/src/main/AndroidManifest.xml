<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SigbuApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSIGBU"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.login.DashboardJefeActivity" />
        <activity android:name=".ui.login.DashboardTrabajadorActivity" />

        <activity android:name=".ui.inventory.InventoryActivity"/>

        <activity
            android:name=".ui.inventory.AddBienActivity"
            android:label="Gestión de Bien"
            android:parentActivityName=".ui.inventory.InventoryActivity" />

        <activity
            android:name=".ui.transfer.TransferActivity"
            android:label="Nueva Transferencia"
            android:parentActivityName=".ui.inventory.InventoryActivity" />

        <activity
            android:name=".ui.requests.RequestsActivity"
            android:label="Solicitudes"
            android:parentActivityName=".ui.login.DashboardJefeActivity"/>
        <activity android:name=".ui.users.ManageWorkersActivity" />
        <activity android:name=".ui.users.AddWorkerActivity" />
        <activity android:name=".ui.location.LocationActivity" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>